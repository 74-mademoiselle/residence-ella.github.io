---
label: Section
hide_body: false
fields:
- name: title
  type: text
  config:
    required: true
  label: Titre
- name: CTA
  type: text
  config:
    required: false
  label: Texte du bouton
- name: services
  type: field_group_list
  fields:
  - name: icon
    type: text
    config:
      required: false
    label: icon
  config:
    min: 
    max: 
    labelField: 
  label: services
